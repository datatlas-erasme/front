import React, { Component } from 'react'

export default class Filter extends Component {
    // Gets Data from FilterSidePanelComp
    // for each filter => get name  + get values to filter and call the button comp in the render
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
