import React, { Component } from 'react'

import Button from './filter-side-panel/Button'

const FilterSidePanel = () => {
    return (
        <div className="FilterSidePanel">
            <Button text="HI IM BUTTON "/>
        </div>

    )
}

export default FilterSidePanel